---
description: MR 治理规范 v1 - 提交前风险检查与 MR 格式要求
globs: ["**/*"]
alwaysApply: true
---

# 提交代码前必须完成

## 0. 读取仓库 Lessons

开始任何修改前，必须读取 `governance/lessons/*.yml`。其中 `enforcement: hard` 是不可违反约束；`enforcement: soft` 是仓库经验和操作提醒。

如果本次修复来自重复错误、门禁失败、线上事故或仓库约束误解，必须补充 `governance/lessons/repository.yml`。能用测试、扫描或门禁验证的写成 `hard` 并绑定可执行检查；暂时只能提醒的写成 `soft`。

## 0. 记录 AI 证据（每次编辑后追加，不靠人工判断 AI 占比）

成段生成代码后向 `.governance/ai-evidence.jsonl` 追加一行：

```json
{"ts":"2026-06-26T10:00Z","tool":"cursor","model":"<模型>","file":"src/Foo.cs","added":40,"removed":2}
```

提交时 `collect_ai_usage.py` 对照实际 diff 自动算 AI-Usage 等级。

> Cursor **Tab 内联补全**混在手敲里、无法精确测行数：只写工具标记（省略 added/removed），脚本判为 `used`（程度未知）。Composer/Chat 整段生成则照实记行数。

## 0.5 跑测试留痕（改了生产代码必做）

改了生产代码就写/补单元测试，用记录器跑：

```bash
python governance/scripts/record_test_run.py -- dotnet test X.Flow.sln --filter Category=Unit
```

提交时 hook 自动写 `Tested:` trailer。本地自检 `python governance/scripts/check_tested.py --staged`。放行：有全绿记录且改了测试文件 / 加 `risk:untested` 注解 / 命中 config 白名单。带失败测试一律拒合。

## 1. 风险扫描

对照 `docs/governance/risk-types.md` 扫描 diff，命中以下任一模式必须加注解：

- **auth-bypass**：字面量 ID/角色与认证字段比较
- **magic-id**：业务代码硬编码 ObjectId / UUID / 长数字串
- **swallowed-exception**：catch 块既不 throw 也不 log
- **suppressed-warning**：`#pragma warning disable` / `// eslint-disable` 等
- **skipped-test**：`[Fact(Skip=...)]` / `it.skip` 等
- **time-bypass**：DateTime 与字面量日期比较
- **env-hardcode**：`if (env == "production")` 等
- **todo-no-context**：TODO/FIXME 不含 (owner, YYYY-MM-DD)

## 2. 加注解格式

```
// risk:<type> reason:"<理由，≥10字，不含：临时/先这样/hack/wip/temp>" owner:@<团队> reviewed:<YYYY-MM-DD>
<命中的代码行>
```

## 3. Commit Trailer（自动生成，勿手填）

仓库装 hook 后（`bash governance/scripts/install-hooks.sh`），提交时自动追加：

```
AI-Usage: heavy
AI-Tools: cursor
AI-Lines: 92/127
Tested: pass (42/42)
```

无需手填。hook 缺失时手动补算：
`python governance/scripts/collect_ai_usage.py --staged --trailer-only`

## 4. 创建 MR（自动生成并提交，勿手填描述）

提交完后走统一入口：

```bash
python governance/scripts/create_mr.py \
  --gitlab-api \
  --why "<任务背景>"
```

`--gitlab-api` 模式直连 GitLab REST API，不依赖 glab/gh CLI（glab 在自托管 GitLab 11.x 上 project 路径解析返回 404）。前置环境变量：`AGENTGATE_GITLAB_TOKEN`（scope: api）、`AGENTGATE_GITLAB_PROJECT_ID`（数字 project id）、`AGENTGATE_GITLAB_URL`。只需提供 `--why`，其余全自动。

## 5. 绝对禁止

- 明文密钥 / token / 私钥
- 删除测试无 `risk:test-removal` 注解
- 修改 `ci/` `CODEOWNERS` `.gitlab-ci.yml` `governance.config.yml` 须人工确认
