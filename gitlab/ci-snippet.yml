# AgentGate GitLab compatibility entrypoint.
#
# Keep this path for existing consumers, but maintain the implementation in
# one place so the published GitLab entrypoints cannot drift apart.
include:
  - local: '/ci/governance-ci.yml'
